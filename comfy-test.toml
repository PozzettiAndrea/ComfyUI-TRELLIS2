[test]
name = "ComfyUI-TRELLIS2"
levels = ["syntax", "install", "registration", "instantiation", "validation"]

[test.workflows]
files = [
    "workflows/geometry_only.json",
    "workflows/geometry_texture.json",
    "workflows/remove_background.json",
]
timeout = 120
